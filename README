In the top level folder, run
make clean && make
export PYTHONPATH=$(pwd)/build:$PYTHONPATH
to be able to use an LSBF from Python3. Note if you want to do this in anaconda,
you should already be running in the virtual environment you plan to use.

To get all test datasets, from the top level folder run
cd data 
./getData

Also note I have only tested this on Ubuntu.

TODO: Add this all to a setup.py folder (use CMake?)
